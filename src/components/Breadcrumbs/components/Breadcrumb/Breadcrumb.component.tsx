import React, { FunctionComponent } from 'react';
import { BreadcrumbProps } from './Breadcrumb.types';

const Breadcrumb: FunctionComponent<BreadcrumbProps> = ({ route, Component, match }) => {
  return (
    <Component>

    </Component>
  );
};

export default Breadcrumb;